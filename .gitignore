# Dependencies
/node_modules

# Build outputs
/build
/dist
/coverage

# Logs
*.log
build.log
npm-debug.log*

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.*

# Package manager
*.tgz

# Screenshots (Playwright testing artifacts)
*.png

# Generated API docs (TypeDoc)
/api-docs

# Bench machine results: latest.json is regenerated by every `npm run bench`;
# only the reviewed reference (baseline.json) is committed.
/bench/results/latest.json

# Starlight docs site build outputs (built by CI)
/docs-site/dist
/docs-site/.astro

# Claude Code
.claude/
.playwright-mcp/
coverage

# Published library: lockfile is dev-only (stripped on publish) and vitest 4.x pulls
# rolldown's per-OS native binding (npm #4828), so a committed cross-OS lock breaks CI.
# NOTE: docs-site/package-lock.json (the deployed docs APP) is intentionally NOT ignored.
/package-lock.json
