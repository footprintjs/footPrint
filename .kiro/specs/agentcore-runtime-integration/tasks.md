# Implementation Plan

- [x] 1. Set up project structure and dependencies
  - Add `@aws-sdk/client-bedrock-runtime` dependency to TreeOfFunctions
  - Add `fast-check` as dev dependency for property-based testing
  - Create `src/agentcore/` directory for new components
  - _Requirements: 1.1, 6.1_

- [x] 2. Implement AskLLM stage factory
  - [x] 2.1 Create Bedrock request formatter
    - Implement `formatBedrockRequest()` to convert messages to Bedrock format
    - Handle system prompt, conversation history, and inference config
    - _Requirements: 1.2, 1.3_
  - [ ]* 2.2 Write property test for conversation history inclusion
    - **Property 1: Conversation history inclusion**
    - **Validates: Requirements 1.3**
  - [x] 2.3 Implement askLLM stage function
    - Create `createAskLLMStage()` factory
    - Initialize Bedrock client with config
    - Execute converse API and parse response
    - _Requirements: 1.1, 1.2, 1.4_
  - [ ]* 2.4 Write unit tests for askLLM stage
    - Test request formatting
    - Test error handling
    - _Requirements: 1.4_

- [x] 3. Implement Tool Registry
  - [x] 3.1 Create tool registry class
    - Implement `register()`, `unregister()`, `getTools()`, `execute()`
    - Store tools in Map by name
    - _Requirements: 5.1_
  - [ ]* 3.2 Write property test for tool definition inclusion
    - **Property 3: Tool definition inclusion**
    - **Validates: Requirements 5.1**
  - [x] 3.3 Integrate tools with askLLM stage
    - Add tool definitions to Bedrock request
    - Handle tool_use responses and execute tools
    - Return tool results to model
    - _Requirements: 5.1, 5.2, 5.3_
  - [ ]* 3.4 Write unit tests for tool execution
    - Test tool invocation flow
    - Test error handling for failed tools
    - _Requirements: 5.2, 5.3_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 5. Implement Memory Adapter
  - [x] 5.1 Create memory event formatter
    - Implement `formatMemoryEvent()` to create AgentCore Memory events
    - Include message content, role, timestamp
    - _Requirements: 2.2_
  - [ ]* 5.2 Write property test for memory event formatting
    - **Property 2: Memory event formatting**
    - **Validates: Requirements 2.2**
  - [x] 5.3 Implement memory adapter class
    - Create `createMemoryAdapter()` factory
    - Implement `loadHistory()` to retrieve events from AgentCore Memory
    - Implement `saveEvent()` to create new memory events
    - Handle connection and operation errors gracefully
    - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - [ ]* 5.4 Write unit tests for memory adapter
    - Test history loading
    - Test event saving
    - Test error handling
    - _Requirements: 2.3, 2.4_

- [x] 6. Implement Runtime Handler
  - [x] 6.1 Create runtime handler wrapper
    - Implement `createRuntimeHandler()` factory
    - Accept FlowBuilder pipeline and optional memory adapter
    - Return AgentCore-compatible handler function
    - _Requirements: 6.1_
  - [ ]* 6.2 Write property test for handler round trip
    - **Property 4: Handler request-response round trip**
    - **Validates: Requirements 6.1, 6.2**
  - [ ]* 6.3 Write property test for session ID propagation
    - **Property 5: Session ID propagation**
    - **Validates: Requirements 6.3**
  - [x] 6.4 Implement request/response handling
    - Parse AgentCoreRequest and extract session ID
    - Execute pipeline with initial context
    - Format AgentCoreResponse from pipeline result
    - _Requirements: 6.2, 6.3_
  - [ ]* 6.5 Write unit tests for runtime handler
    - Test request parsing
    - Test response formatting
    - _Requirements: 6.2_

- [ ] 7. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 8. Implement Observability Enhancements
  - [x] 8.1 Add execution metadata tracking
    - Ensure stage timing is recorded in context
    - Add status tracking for each stage
    - _Requirements: 7.1_
  - [ ]* 8.2 Write property test for execution metadata completeness
    - **Property 6: Execution metadata completeness**
    - **Validates: Requirements 7.1**
  - [x] 8.3 Add error metadata tracking
    - Ensure errors are captured in context tree
    - Include error message and stack trace
    - _Requirements: 7.3_
  - [ ]* 8.4 Write property test for error metadata inclusion
    - **Property 7: Error metadata inclusion**
    - **Validates: Requirements 7.3**

- [ ] 9. Create exports and documentation
  - [ ] 9.1 Update index.ts exports
    - Export all new components from `src/index.ts`
    - _Requirements: All_
  - [ ] 9.2 Add JSDoc comments
    - Document all public interfaces and functions
    - Include usage examples in comments
    - _Requirements: All_

- [ ] 10. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
